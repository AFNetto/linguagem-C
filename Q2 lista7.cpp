//ler matrizes e fazer a multiplicação entre os dois
#include<stdio.h>
#include<stdlib.h>
#define tam 2
int main()
{
	int i, j, k;
	int matriz[tam][tam];
	printf("digite os valores da matriz\n")
	printf("digite a oredem da matriz( com maximo 2)\n")
	scanf("%d", k)
	for(i = 0; i <= k; i++)
		scanf("%d", matriz[i][j]);
}
int matriz()
{
	int l, m, n;
	int matriz1[tam][tam];
	printf("digite os valores da matriz\n")
	printf("digite a oredem da matriz( com maximo 2)\n")
	scanf("%d", n)
	for(l = 0; l <= n; i++)
		scanf("%d", matriz1[l][m]);
}
int multiplicacao()
{
	int mult;
	for(x = 0; x <= 2; x++)
		mult = matriz[i][j] * matriz1[l][m];
	printf("%d""%d""%d"matriz, matriz1, mult);
}
