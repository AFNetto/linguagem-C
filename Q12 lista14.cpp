// Outro exemplo para ponteiros.
#include<stdio.h>
#include <stdlib.h>
void(int[]);
int_impressao(int)
#define TAM 500
int main ()
{
	int vetor[TAM];
	printf("o vetor\n");
	exibir int_impressao;
}

void(int[])
{
	int i, *pt, n;
	printf("digite o numero de termos\n");
	scanf("%d", & n)
	printf("digite os valores do vetor\n");
	for(i = 0; i <= n, i++)
	{
		scanf("%d", &vetor[i])
	}
}

int_impressao(int)
{
	int *pt;
	pt = &vetor[i];

	return *pt;
}
